bin/bash
echo 'Começaremos os downloads'
sudo apt update;
sudo apt upgrade;
echo 'Agora podemos instalar o apache'
sudo apt install mysql-server;
sudo apt install php;
sudo apt install phpmyadmin;
sudo apt install apache2;

